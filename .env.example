# LM Studio Proxy Configuration

# Port for the proxy server (default: 11434 to match Ollama)
PORT=11434

# LM Studio base URL (default: http://localhost:1234)
# Change this if LM Studio is running on a different port or host
LMSTUDIO_BASE_URL=http://localhost:1234

# Optional: Set log level (error, warn, info, debug)
LOG_LEVEL=info